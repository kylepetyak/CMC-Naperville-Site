import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Camelback Medical Centers – Naperville | Expert Chiropractic Care',
  description: 'Premier chiropractic care in Naperville, IL. Expert treatment for back pain, neck pain, headaches, and sports injuries. Same-day appointments available.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
}
